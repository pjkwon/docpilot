# LLM 벤치마크 작업 기록 — 2026-07-27

Ollama 컨텍스트 윈도우 이슈 대응부터 로컬 모델(qwen2.5:7b vs gemma4:e4b) 비교 시도까지의 기록.
결론부터: **모델 간 정량 비교는 아직 확보하지 못했다** — 두 모델 다 첫 실행에서 실패했고, 실패 원인은
서로 다른 두 가지 별개 문제였다.

## 1. 배경 — Ollama 컨텍스트 윈도우 문제

Ollama는 `num_ctx`를 명시하지 않으면 모델이 실제 지원하는 컨텍스트보다 훨씬 작은 값으로 로드하고,
초과분을 에러 없이 조용히 잘라낸다. RAG로 검색된 컨텍스트가 이 기본값을 넘으면 응답이 중간에
끊기거나 앞부분 근거가 사라질 수 있는데도 신호가 없다는 게 핵심 리스크였다.

## 2. 구현 — v0.2.3 (배포 완료)

| 항목 | 내용 |
|---|---|
| `num_ctx` 지원 | `DocPilot(llm="ollama", num_ctx=...)` — 요청마다 Ollama에 전달, 예상 토큰이 예산 초과 시 API 호출 전 `ContextExceededError`로 즉시 차단 |
| 자동 재시도 | `RagMapper.map()`이 `ContextExceededError` 발생 시 `top_k`를 절반씩 줄여 최대 2회 재시도 (10→5→2). Ollama 사전 차단 + Claude/OpenAI/Gemini/Grok의 실제 API 거부 에러 양쪽에 공통 적용 |
| 타임아웃 | Ollama 매퍼 기본 요청 타임아웃 180초 — VRAM 부족 시 에러 없이 CPU로 전환되며 20~50배 느려지는(무응답처럼 보이는) 상황에 대한 안전장치 |
| 배포 | PyPI `smart-docgen==0.2.3` (태그 `v0.2.3`, GitHub Actions `publish.yml`로 자동 빌드/배포) |

**설계에서 폐기한 방향**: "예산 초과 시 `num_ctx`를 올려서 재시도"는 검토 후 제외했다. Ollama가
VRAM 초과분을 에러 없이 CPU로 스필오버시키기 때문에(20~50배 느려짐, 잡을 에러 자체가 없음),
`num_ctx`를 자동으로 올리는 재시도는 실패 신호가 없어 트리거될 수 없고 하드웨어 여유분에 기대는
도박이 된다. 대신 우리가 완전히 통제 가능한 `top_k` 축소만 재시도 대상으로 삼았다.

## 3. 실측 환경 확인

`ollama ps` / `ollama show`로 실제 로드 상태를 직접 조회했다 (일반론이 아닌 이 머신 기준 실측):

| 모델 | 아키텍처 최대 컨텍스트 | 실제 로드된 값 | PROCESSOR |
|---|---|---|---|
| qwen2.5:7b | 32,768 | (조회 시점 미로드) | — |
| gemma4:e4b | 131,072 | **4,096** | **100% CPU** |

`num_ctx`를 지정하지 않으면 gemma4:e4b도 128k 지원 모델임에도 4096으로만 로드된다는 걸 확인했고,
GPU가 전혀 안 잡히고 100% CPU로 돌고 있다는 것도 이때 발견했다 — 이후 벤치마크 실패의 직접 원인이 됐다.

## 4. 벤치마크 스크립트

[scripts/llm_benchmark.py](scripts/llm_benchmark.py) 신규 작성. `DocPilot.benchmark()`로 동일한
RAG 검색 결과를 여러 Ollama 모델에 동일하게 넣어 처리시간·토큰 사용량·섹션별 생성 내용을 비교한다.

```
uv run python scripts/llm_benchmark.py --models qwen2.5:7b,gemma4:e4b
```

템플릿: `data/minutes.docx` (회의록) / 소스: `data/2025.11.19 도장 설비 업체 화상회의.docx`

## 5. 1차 실행 결과 — 둘 다 실패

```
모델            처리시간(s)   상태
qwen2.5:7b        0.00      오류: Failed to parse LLM response as JSON
gemma4:e4b        0.00      오류: API call failed — Request timed out.
```

### gemma4:e4b — 180초 타임아웃

100% CPU로 돌고 있어 응답 생성이 180초 안에 끝나지 않았다. 이번에 구현한 타임아웃 안전장치가
의도대로 작동한 사례지만, 비교용 결과 자체는 얻지 못했다. GPU 미사용 원인(드라이버/설정 문제인지,
이 머신에 애초에 GPU가 없는지)은 확인하지 않았다.

### qwen2.5:7b — JSON 파싱 실패 (비결정적)

응답은 받았지만 파싱에 실패했다. 잘려나온 로그 앞부분:
```json
"회의록 제목": "[스타일: 18pt, 볼드, CENTER]2025년 11월 28일 회의록",
"YYYY년 MM월 DD일 HH:MM": "[스타일: 표 셀 너비 76mm]2025년 11월 28일 14:30",
```
프롬프트의 `[스타일: ...]`는 "이 서식을 참고해서 분량을 정하라"는 지시문인데, 모델이 이걸 지시가
아니라 값 안에 그대로 베껴 넣었다. 날짜도 소스 문서(11/19)와 다른 11/28로 나와 있어, 다른 이상
징후와 겹쳐 있었다.

## 6. 재현 시도 — qwen2.5:7b 단독 재실행

같은 프롬프트로 qwen2.5:7b만 다시 호출했다. 이번엔 **완전히 정상적인 JSON**이 나왔다
(`finish_reason=stop`, 1163자, 파싱 성공, 날짜도 정확히 11/19로 나옴, `[스타일: ...]` 텍스트 누출 없음).

**결론**: 코드나 프롬프트의 재현 가능한 버그가 아니라 **모델의 비결정적 생성 편차**다. temperature가
0이 아닌 상태로 돌아가고 있어, 같은 입력에도 실행마다 "서식 힌트를 지시로 이해"할지 "값에 그대로
포함"할지가 갈린다. 7B급 로컬 모델에서 흔한 지시 따르기(instruction-following) 신뢰성 문제로 보인다.

## 7. 미해결 항목

- **gemma4:e4b가 GPU를 못 잡는 원인 미조사** — 드라이버, Ollama 설정, 하드웨어 자체 중 어느 쪽인지 확인 필요. 확인 전까지는 CPU 전용 실행 전제로 타임아웃을 늘려야 비교가 가능함.
- **qwen2.5:7b JSON 신뢰성 문제 미해결** — 검토한 대응 방향 셋:
  1. temperature를 낮추거나 seed 고정 (출력을 더 결정적으로 — 완전히 없애지는 못함)
  2. 프롬프트에 "브래킷 힌트를 값에 그대로 복사하지 말라"를 명시적으로 추가 (현재 프롬프트는 "복사 금지"를 명시하지 않음)
  3. JSON 파싱 실패 시 재시도 추가 — 현재 `RagMapper`는 `ContextExceededError`만 재시도하고, 일반 JSON 파싱 실패는 재시도 없이 바로 실패함
- **qwen2.5:7b vs gemma4:e4b 정량 비교(처리시간/토큰량/내용 품질) 자체가 아직 없음** — 위 두 문제를 먼저 해결해야 재실행이 의미 있는 비교가 됨.
